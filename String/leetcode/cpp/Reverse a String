funtion to reverse string in gfg easy problem

 string revStr(string s) {
        // code here
        int n = s.size();
        int size ;
        if(n%2 == 1){
            size = n/2+1;
        }
        else{
            size = n/2;
        }
        for(int i = 0; i < size; i++){
            swap(s[i],s[(n-1) - i]);
        }
        return s;
    }
